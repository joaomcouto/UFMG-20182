O exercício consiste de uma lista encadeada simples com operação de inserir elementos e imprimir elementos. O código possui erros de compilação, execução, e vazamentos de memória (memory leak), você deve consertar todos.

Os módulos do código são:

Node: elemento da lista, possui um float e um apontador para o próximo, um construtor, destrutor e métodos de get e set desses atributos.

ListaEncadeada: Lista de nodes, possui um único atributo _inicio (apontador para o Node inicial), construtor, destrutor, métodos insere_node (são dois métodos diferentes com o mesmo nome, utilizando polimorfismo) e imprime_lista.

Main: PODE SER ALTERADO, PORÉM SERÁ SUBSTITUÍDO PELO ORIGINAL AO AVALIAR.

Ignore a linha: "still reachable: 72,704 bytes in 1 blocks" do Valgrind. A mesma é um falso positivo que ocorre no ambiente moodle.

Use o Debugger da sua máquina ou do moodle para resolver os problemas.

No main supomos o o uso da lista. Inicie com um breakpoint aqui.  Faz parte do exercício a interpretação e compreensão mais detalhada do código.

Usando o DDD. Veja o vídeo abaixo com exemplos de como fazer uso do DDD.
