#include <iostream>
#include "listaencadeada.h"
#include "node.h"
#include "BST.h"


int main(){
    /* Em ordem eh imprimir tudo a esquerda, o no do meio e tudo da direita , ordena os valorse da arvore
        std::cout<<this->est->elem
        if (this->esq != nullptr) this->esq->preordem
        std::cout<<this->elemento
        if (this->dir != null) this->dir->preordem
        if this this nao eh null prt cahama pre ordem nele
        
        
        Pos ordem imprime as folhas da arvore inicialmente, bom pra destruir arvore
        
        
        Pre ordem serve pra duplicaa a arvore 
        
        Em todos os ordens voce chama pra esquerda e pra direita, a unica diferenca eh aonde esta o print de elemento
            Antes de tudo eh o pre ordem
            Entre as duas chamas eh em ordem
            Depois das duas eh pos ordem
            
No pre ordem do node 
void Node::_pre_ordem(ListaEncade l){
    l.adicionar_elemento(thos->elemento);
    se esquerda nao for nullptr chama funcao pra esqeurda
    se direita nao for nullptr chama funcao pra direita
    
}


ListaEncadeada Node::pre_ordem(){
    ListaEncadeada l ;
    this->_pre_ordem(l);
    return l ;
}
        
        
    */
    return 0;
}